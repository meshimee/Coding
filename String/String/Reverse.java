package String;

public class Reverse {
          public static void main(String[] args) {
                    ///String s = "i.like.this.program.very.much";     //i ekil siht margorp yrev hcum
                    String s = "i like this program very much";       //i ekil siht margorp yrev hcum


                     
                    //  StringBuilder sb = new StringBuilder();
                    //  StringBuilder ss = new StringBuilder();
                    // for(char word : s.toCharArray()){
                    //          sb.append(word);
                             
                    //          ss = sb.appned(sb.reverse(););
                    // }     
                    // }
                    //String str= "My name is Subrato";

                    // StringBuilder reversedStr=(new StringBuilder(s)).reverse();
                    // int i = 0;
                    // int j = reversedStr.length()-1;

                    // while(i<=j){
                    //           char word = reversedStr.charAt(i);
                    //           reversedStr.charAt(i) = reversedStr.charAt(j);
                    //           reversedStr.charAt(j) = word;
                    //           i++;
                    //           j--;
                    // }
                    // for(int i = reversedStr.length()-1 ; i <= 0 ; i--){
                    //           System.out.print(reversedStr.charAt(i)); 
                    // }                    
          }
}
